@page
@model BasketballChallenge.Pages.Games.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Game[0].DateofGame)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Game[0].Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Game[0].UserPaid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Game[0].AmountPaid)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Game) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateofGame)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserPaid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AmountPaid)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit Details</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">View Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete Details </a>
            </td>
        </tr>
}
    </tbody>
</table>
